{
  "name": "hanlu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hànlù — Chinese learning app. Static frontend (index.html) + optional serverless AI tutor (api/chat.js).",
  "scripts": {
    "dev": "python3 -m http.server 8777 --bind 127.0.0.1",
    "start": "python3 -m http.server 8777 --bind 127.0.0.1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "openai": "^6.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
