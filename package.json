{
  "name": "hanlu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hànlù — Chinese learning app. Static frontend (index.html) with a FREE in-browser AI tutor (WebLLM/WebGPU, loaded from CDN). This package.json only declares deps for the OPTIONAL paid backend (api/chat.js); the app needs no build step or npm install to run.",
  "scripts": {
    "dev": "python3 -m http.server 8777 --bind 127.0.0.1",
    "start": "python3 -m http.server 8777 --bind 127.0.0.1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "openai": "^6.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
